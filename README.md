# CmHazel
Learning game engine design based on The Cherno's game engine videos

# Next Step
- Refactoring the engine code
- Use Vulkan as the engine's primary rendering interface
- The runtime for script interactions may be changed depending on the system.
  - Windows, Linux, and macOS will use .Net as the runtime
  - while others such as Android and iOS will continue to use Mono.

# Schedule
Can only be done as a part-time job
